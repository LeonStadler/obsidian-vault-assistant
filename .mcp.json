{
  "mcpServers": {
    "obsidianVaultFilesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "${OBSIDIAN_VAULT_PATH}"
      ]
    }
  }
}
