interface:
  display_name: "Vault Context"
  short_description: "Summarize vault-backed context"
  default_prompt: "Use $vault-context to find relevant Obsidian notes, summarize the current facts, and cite the note paths."

policy:
  allow_implicit_invocation: true
