interface:
  display_name: "Vault Structure"
  short_description: "Repair hubs and note structure"
  default_prompt: "Use $vault-structure to improve Obsidian hubs, folder notes, navigation links, and canonical note placement."

policy:
  allow_implicit_invocation: true
